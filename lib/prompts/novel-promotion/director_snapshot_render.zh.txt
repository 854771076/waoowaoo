你是 3D 导演台到分镜图的视觉导演。请根据导演台快照，把单个分镜渲染成一张完整画面。

输出比例：{aspect_ratio}
整体风格：{style}

原文/台词：
{source_text}

分镜与导演台快照 JSON：
{storyboard_text_json_input}

硬性要求：
- `director_snapshot` 与 `panel.director_shot` 是最高优先级构图依据，必须遵循快照中的机位 FOV、相机位置、目标点、角色站位、姿态、朝向、前后层次。
- 快照里的假人模型只表示人物走位、体型、姿势和朝向；最终画面必须生成真实角色，不要画成灰模、假人、玩偶或 3D 视口截图。
- 若提供快照参考图，它用于锁定构图和空间关系；角色身份、服装、发型、道具和场景细节仍以项目角色/场景参考图与 JSON 描述为准。
- 不要改变镜头视角，不要擅自重排角色，不要把相机、控制器、网格线、坐标轴、UI、文字标签画进画面。
- 如果角色参考图与快照站位冲突，站位和构图以快照为准，外观以角色参考图为准。
- 画面必须是可直接用作小说推广分镜的成片感插画/影视帧。
